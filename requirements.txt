# requirements.txt — Ambiente Python per il progetto di tesi
# Testato con Python 3.10 su CPU (locale) e GPU Tesla T4 (Google Colab)
# Per installare: pip install -r requirements.txt

# === Core scientifico ===
numpy>=1.24,<2.0
scipy>=1.11
scikit-image>=0.21
scikit-learn>=1.3
opencv-python>=4.8
Pillow>=10.0
matplotlib>=3.7
pandas>=2.0

# === Deep Learning (PyTorch — versione CPU) ===
# Per GPU: sostituire con torch==2.1.0+cu118 --index-url https://download.pytorch.org/whl/cu118
torch>=2.1
torchvision>=0.16

# === Segmentazione (benchmark Colab) ===
# cellpose>=3.0   # Solo su Colab/GPU — non installare in locale CPU

# === Machine Learning e Interpretabilità (Fase 4) ===
xgboost>=2.0
shap>=0.44

# === Utility ===
tqdm>=4.66
joblib>=1.3
